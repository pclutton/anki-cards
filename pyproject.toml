[project]
name = "anki-cards"
version = "0.1.0"
description = "Generate Anki flashcard decks from PDF lecture notes using the Claude API"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "anthropic>=0.120.0",
    "genanki>=0.13.1",
    "pymupdf>=1.27.2.3",
    "pymupdf4llm>=1.27.2.3",
    "python-dotenv>=1.2.2",
]
